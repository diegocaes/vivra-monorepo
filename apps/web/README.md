# Vivra web

Astro SSR, React components, Tailwind CSS, and Supabase. Install and run with pnpm
from the monorepo root. See the [root README](../../README.md) for setup and checks.

```sh
pnpm --filter web dev --host 127.0.0.1
pnpm smoke:web
pnpm build:web
```
